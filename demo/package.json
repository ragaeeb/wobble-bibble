{
    "dependencies": {
        "lit": "^3.3.2",
        "wobble-bibble": "^1.4.5"
    },
    "devDependencies": {
        "typescript": "~5.9.3",
        "vite": "^8.0.0-beta.14"
    },
    "name": "demo",
    "overrides": {
        "vite": "^8.0.0-beta.13"
    },
    "private": true,
    "scripts": {
        "build": "tsc && vite build && cp CNAME dist/",
        "deploy": "bun run build && surge ./dist --domain wobble-bibble.surge.sh",
        "dev": "vite",
        "preview": "vite preview"
    },
    "type": "module",
    "version": "0.0.0"
}
